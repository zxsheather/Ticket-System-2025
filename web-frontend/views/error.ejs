<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || '错误' %> - 票务系统</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- 自定义样式 -->
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- 导航栏 -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-train me-2"></i>票务系统
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">首页</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- 主要内容 -->
    <main class="container my-4" style="padding-top: 80px;">

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
      <div class="card text-center fade-in">
        <div class="card-body py-5">
          <div class="error-icon mb-4">
            <% if (status === 404) { %>
              <i class="fas fa-map-marker-alt text-warning" style="font-size: 5rem;"></i>
            <% } else if (status === 403) { %>
              <i class="fas fa-ban text-danger" style="font-size: 5rem;"></i>
            <% } else if (status === 500) { %>
              <i class="fas fa-tools text-warning" style="font-size: 5rem;"></i>
            <% } else { %>
              <i class="fas fa-exclamation-triangle text-warning" style="font-size: 5rem;"></i>
            <% } %>
          </div>
          
          <h1 class="display-4 text-muted mb-3">
            <%= status || '错误' %>
          </h1>
          
          <h3 class="mb-4">
            <%= title %>
          </h3>
          
          <p class="lead text-muted mb-4">
            <%= message %>
          </p>
          
          <% if (typeof stack !== 'undefined' && stack) { %>
            <div class="alert alert-secondary text-start mt-4">
              <details>
                <summary class="mb-2">
                  <strong>详细错误信息 (开发模式)</strong>
                </summary>
                <pre class="mb-0"><%= stack %></pre>
              </details>
            </div>
          <% } %>
          
          <div class="d-grid gap-2 d-md-flex justify-content-md-center">
            <button class="btn btn-outline-primary" onclick="history.back()">
              <i class="fas fa-arrow-left"></i> 返回上页
            </button>
            <a href="/" class="btn btn-primary">
              <i class="fas fa-home"></i> 返回首页
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- 自定义脚本 -->
    <script src="/js/main.js"></script>
</body>
</html>
